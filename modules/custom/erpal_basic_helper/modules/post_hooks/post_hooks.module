<?php

/**
* @file provides hook functions to let other modules alter some hook result even if their weight will let them process the hook bevor
* other modules that provide the hook.
*/

function post_hooks_node_view($node) {
  if (is_object($node))
    module_invoke_all('node_view_post', $node);
}