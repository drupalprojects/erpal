<?php

/**
 * Implements hook_bssync_plugin
 * @return type 
 */
function erpal_contact_bssync_plugin() {
  
  return array(
    'erpal_contact'  => array(
        'type'   => 'contact',
        'title'  => t('Erpal Contact Plugin'),
        'form'   => 'erpal_contact_plugin_form',
        'getids' => '_erpal_contact_getids',
        'write'  => '_erpal_contact_write',
        'get'    => '_erpal_contact_get',
    )
  );
}


function erpal_contact_plugin_form() {
  
  $form = array();
  $form['info'] = array(
      '#markup' => '<h2>Erpal Contact Plugin</h2>',
  );
  return $form;
}

function _erpal_contact_write($id) {
  
}

function _erpal_contact_get($id) {
  
}